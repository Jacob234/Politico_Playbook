# Virtual Environments
.venv/
venv/
env/
ENV/

# Environment variables
.env
*.env

# Sensitive data
credentials/
credentials
credentials.json
token.json
*.key
*.pem
*.p12
*.pfx
config/secrets/
secrets/

# OS Files
.DS_Store
**/.DS_Store
Thumbs.db

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Logs
*.log
logs/

# Data (optional - decide if you want to track)
data/cache/
data/raw/*.html
data/raw_emails.db
data/raw_emails.db-journal
data/raw_emails.db-wal
data/raw_emails.db-shm
*.db
*.db-journal
*.db-wal
*.db-shm

# User scratch / drafts
Schema.rtf

# OAuth client secrets (NEVER commit)
oauth_client.json

# Claude Code per-machine settings (permissions allowlist, tool defaults).
# Project-shared Claude Code config, if any, would go in .claude/settings.json (no .local).
.claude/settings.local.json
